# SPA fallback for dynamic chat routes
# These routes are not pre-rendered, so we serve index.html and let client-side routing handle them

/c/*    /index.html    200
/ko/c/*    /ko/index.html    200

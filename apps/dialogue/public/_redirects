# SPA fallback for dynamic chat routes
# These routes are not pre-rendered, so we serve __spa-fallback.html and let client-side routing handle them

/c/*    /__spa-fallback.html    200
/ko/c/*    /__spa-fallback.html    200
